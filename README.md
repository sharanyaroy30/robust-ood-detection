# robust-ood-detection
A deep learning project exploring out-of-distribution (OOD) detection in neural networks. Implements and compares multiple uncertainty estimation methods, including softmax confidence, Monte Carlo dropout, and energy-based scoring, to identify when models encounter unseen data.
